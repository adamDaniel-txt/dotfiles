#!/bin/sh

basename="${1%.*}"

case "${*}" in
	*.html) setsid -f "$BROWSER" "$basename".html >/dev/null 2>&1 ;;
	*.sent) setsid -f sent "$1" >/dev/null 2>&1 ;;
	*.md) setsid -f zathura "${1%.md}.pdf" >/dev/null 2>&1 ;;
esac
